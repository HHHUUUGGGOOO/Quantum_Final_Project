import numpy as np
# M = np.identity(2)
# N = np.zeros([2, 2])
# O = np.identity(2)
# P = np.zeros([2, 2])
# A = np.hstack((M, N))
# B = np.hstack((P, O))
# C = np.vstack((A, B))
# print(C)
a = -3
print(abs(a))
